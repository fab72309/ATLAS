# Copiez ce fichier en `.env` ou `.env.local` puis remplissez les valeurs.

# --- Firebase (obligatoire pour les sauvegardes/IA côté serveur) ---
VITE_FIREBASE_API_KEY=FAKE_KEY
VITE_FIREBASE_AUTH_DOMAIN=example.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=example
VITE_FIREBASE_STORAGE_BUCKET=example.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=0000000000
VITE_FIREBASE_APP_ID=1:0000000000:web:deadbeefdeadbeef
VITE_FIREBASE_MEASUREMENT_ID=

# --- Météo (optionnel pour la fonctionnalité météo du zonage) ---
# Clé depuis https://www.weatherapi.com/ (gratuit)
VITE_WEATHER_API_KEY=

# --- OpenAI via proxy (obligatoire en prod) ---
# Mettez l'URL HTTPS de la Function `analyze` (Firebase Functions).
VITE_OPENAI_PROXY_URL=

#
# La clé OpenAI reste côté serveur (Firebase Functions).
